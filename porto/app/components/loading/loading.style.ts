import { ViewStyle } from 'react-native';

const CONTAINER: ViewStyle = {
  flex: 1,
  justifyContent: 'center',
};
const HORIZONRAL: ViewStyle = {
  flexDirection: 'row',
  justifyContent: 'space-around',
  padding: 10,
};

export const styles = {
  CONTAINER,
  HORIZONRAL,
};
